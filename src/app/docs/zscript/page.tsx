import Link from "next/link";
import { ArrowRight, Archive, ShieldCheck } from "lucide-react";

export default function ZScriptMigrationPage() {
  return <main className="min-h-screen bg-background px-4 py-12 text-foreground sm:px-8"><section className="mx-auto max-w-3xl border hairline bg-panel p-6 sm:p-9"><div className="inline-flex items-center gap-2 text-[10px] uppercase tracking-[0.16em] text-warn"><Archive className="h-3.5 w-3.5" />Archived language</div><h1 className="mt-3 text-3xl font-semibold tracking-tight">ZScript is retired for new research.</h1><p className="mt-4 max-w-2xl text-sm leading-7 text-muted-foreground">ZTerminal has moved new indicators and strategies to the Python Research API. Existing ZS source and prior run records remain archival/exportable during migration, but ZS no longer represents the recommended authoring, validation, or execution path.</p><div className="mt-6 grid gap-3 sm:grid-cols-2"><div className="border hairline bg-surface/40 p-4"><ShieldCheck className="h-4 w-4 text-mdata" /><h2 className="mt-2 text-sm font-medium">What changes</h2><p className="mt-1 text-[11px] leading-5 text-muted-foreground">New work uses constrained Python, an isolated worker, deterministic Rust execution, provider-labelled data manifests, and explicit Pine conversion review.</p></div><div className="border hairline bg-surface/40 p-4"><Archive className="h-4 w-4 text-warn" /><h2 className="mt-2 text-sm font-medium">What remains</h2><p className="mt-1 text-[11px] leading-5 text-muted-foreground">Historical ZS source is kept as an audit record. It is never silently reinterpreted as Python and cannot create new research runs.</p></div></div><Link href="/docs/python-research" className="mt-7 inline-flex items-center gap-2 rounded-[4px] bg-research px-3 py-2 text-xs font-medium text-research-foreground hover:bg-research/90">Open Python Research API documentation <ArrowRight className="h-3.5 w-3.5" /></Link></section></main>;
}
