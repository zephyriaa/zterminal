# Deployment Workflow

Whenever coding tasks or changes are completed, tested, and verified:
1. Stage all relevant modified and new files.
2. Commit with a clean, conventional commit message describing the changes.
3. Push to `origin main` to trigger the production deployment automatically (Render / CI).
