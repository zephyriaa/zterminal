# Landing demo captures — 2026-09-23

Source: https://zterminal-web.zephyria-inc.workers.dev/terminal

The three product demo images are fresh, unmodified browser screenshots from the currently deployed workstation, captured at a 1600 × 1000 CSS viewport with device scale factor 2 (3200 × 2000 image pixels).

- product-market-live.png: the default chart workspace, with actual BTC/USDT history and current market-context state.
- product-strategy-live.png: Python Strategy selected; the context panel was closed and the divider resized through the application's UI to give the current editor room.
- product-research-live.png: Research selected in the same resized workspace, showing its archive and report tabs.

No historical screenshot, generated UI, fabricated performance result, or substituted market feed was used. The live depth feed reported disconnected and the local Helper was unavailable in the capture session. The captions therefore describe the actual views rather than asserting connected feeds, verified runs, or specific returns.

The approved laptop hero composition is unchanged. These assets replace the three product demo screenshots below it.

Header refinement: the canonical mark's image box was reduced from 68px to 52px (about 24%) on the landing header. Shared header controls now use a brief press compression, contact-position light highlight, smooth release, keyboard focus feedback, and a static reduced-motion response. The mobile menu's second line correctly rotates into the close icon.
